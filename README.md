# AI Code Detector

AI Code Detector is a web application and API for analyzing whether a source-code snippet is likely written by a human or generated by AI. The project is based on the SemEval-2026 Task 13 setting and currently supports three prediction subtasks using locally stored UniXcoder-based models.

## Current Status

This repository is an active development project. The core backend inference API and React web interface are implemented. Authentication, user storage, Docker deployment, file upload, batch scanning, and span-level highlighting are not included in the current version.

## Features

- Detect whether code is human-written or AI-generated.
- Attribute AI-generated code to a likely model family.
- Classify code style as human, machine, hybrid, or adversarial.
- Display prediction confidence in a web UI.
- Store recent analysis history in browser local storage during the current session.
- Download prediction results as JSON from the browser or backend.

## Project Structure

```text
Code_Detection/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI application and endpoints
│   │   ├── model_service.py     # Model loading and prediction logic
│   │   └── schemas.py           # Pydantic request/response schemas
│   ├── models/
│   │   ├── unixcoder_subtask_a/
│   │   ├── unixcoder_subtask_b/
│   │   └── unixcoder_subtask_c/
│   └── requirements.txt
├── web/
│   ├── src/
│   │   ├── App.jsx
│   │   ├── api/
│   │   ├── components/
│   │   └── utils/
│   ├── package.json
│   └── vite.config.js
├── Notebook/
│   ├── Semeval_2026_Task13_Subtask_A_Train.ipynb
│   ├── Semeval_2026_Task13_Subtask_B_Train.ipynb
│   ├── Semeval_2026_Task13_Subtask_C_Train.ipynb
│   └── subtask-a-train.ipynb
├── run_project.sh
└── README.md
```

## Model Tasks

| Task | Purpose | Labels |
| --- | --- | --- |
| Subtask A | Binary code-origin detection | `Human`, `AI` |
| Subtask B | Generator-family attribution | `Human`, `DeepSeek-AI`, `Qwen`, `01-ai`, `BigCode`, `Gemma`, `Phi`, `Meta-LLaMA`, `IBM-Granite`, `Mistral`, `OpenAI` |
| Subtask C | Generation-style classification | `Human`, `Machine`, `Hybrid`, `Adversarial` |

The current models are fine-tuned from `microsoft/unixcoder-base` and loaded from:

```text
backend/models/unixcoder_subtask_a/best_model
backend/models/unixcoder_subtask_b/best_model
backend/models/unixcoder_subtask_c/best_model
```

## Tech Stack

### Backend

- Python
- FastAPI
- Pydantic
- PyTorch
- Transformers
- Uvicorn

### Frontend

- React
- Vite
- JavaScript
- CSS
- Axios

### Training

- Jupyter Notebook
- Hugging Face Transformers
- Hugging Face Datasets
- UniXcoder

## Requirements

- Python 3.11 or newer
- Node.js 20 or newer
- npm
- `gnome-terminal`
- `xdg-open`
- Local model files under `backend/models/`

## Setup And Run

From the repository root, run:

```bash
./run_project.sh
```

The script starts all required local services:

- FastAPI backend on `http://localhost:8000`
- React frontend on `http://localhost:5173`

It also opens the frontend in your browser.

Backend API documentation is available at:

```text
http://localhost:8000/docs
```

If this is the first time running the project, make sure dependencies were installed once:

```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

cd ../web
npm install
```

After that, day-to-day startup only needs:

```bash
./run_project.sh
```

## Startup Script

`run_project.sh` performs these steps:

- Starts the FastAPI backend in a new terminal.
- Starts the React frontend in a new terminal.
- Opens `http://localhost:5173`.

Note: this script is intended for a local Linux desktop environment with `gnome-terminal` and `xdg-open` installed.

## API Endpoints

### Health Check

```http
GET /health
```

Example response:

```json
{
  "status": "ok",
  "available_models": ["subtask_a", "subtask_b", "subtask_c"]
}
```

### Predict All Tasks

```http
POST /predict-all
```

Request body:

```json
{
  "code": "def add(a, b):\n    return a + b",
  "language": "python"
}
```

Example response:

```json
{
  "input": {
    "language": "python",
    "code": "def add(a, b):\n    return a + b"
  },
  "predictions": {
    "subtask_a": {
      "label": 1,
      "prediction": "AI",
      "confidence": 0.91,
      "summary": "AI - 91.00%",
      "probabilities": {
        "Human": 0.09,
        "AI": 0.91
      }
    }
  }
}
```

When all three models are available, the `predictions` object contains `subtask_a`, `subtask_b`, and `subtask_c`.

### Download Backend JSON

```http
POST /predict-all-json
```

This endpoint runs the same prediction pipeline, writes a JSON file under `outputs/`, and returns it as a downloadable file.

## Frontend Workflow

1. Paste source code into the input area.
2. Select the programming language.
3. Click `Analyze Code`.
4. Review prediction cards for each available subtask.
5. Optionally show raw JSON or download the result.

The current UI stores history using browser local storage under the key:

```text
analyze_history
```

## Training Notebooks

Training experiments are stored in `Notebook/`.

The notebooks fine-tune `microsoft/unixcoder-base` for the SemEval subtasks and export Hugging Face-compatible model folders containing files such as:

- `config.json`
- `model.safetensors`
- `tokenizer.json`
- `tokenizer_config.json`
- `id2label.json`
- `label2id.json`
- `model_meta.json`

Validation metrics saved in the model metadata:

| Task | Validation Accuracy | Validation F1 |
| --- | ---: | ---: |
| Subtask A | 0.9964 | 0.9964 |
| Subtask B | 0.9514 | 0.6302 |
| Subtask C | 0.9175 | 0.8745 |

## Limitations

- Predictions are probabilistic and should not be treated as absolute proof of authorship.
- Very short code snippets may produce unreliable predictions.
- Edited AI-generated code can be harder to classify correctly.
- Model-family attribution is approximate.
- The selected programming language is currently included in the response but is not used by the model inference logic.
- The backend truncates input to 512 tokens during model inference.

## Public Deployment

To make the app usable by anyone with a link, deploy two parts:

- Frontend: deploy the `web/` Vite app to a static hosting service.
- Backend: deploy the `backend/` FastAPI app to a Python server with enough memory for the model files.

### Backend Environment Variables

Set these variables on the backend hosting service:

```text
CORS_ALLOW_ORIGINS=<your frontend public URL>
```

Example:

```text
CORS_ALLOW_ORIGINS=https://your-app.example.com
```

Backend start command:

```bash
uvicorn app.main:app --host 0.0.0.0 --port $PORT
```

If the platform does not provide `$PORT`, use:

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

Important: the backend needs the model folders under `backend/models/`. Because `.gitignore` currently ignores model folders, make sure the deployment platform receives these files through one of these approaches:

- commit or upload the model files intentionally,
- attach persistent storage containing `backend/models/`,
- or download/copy the model files during the deploy process.

### Frontend Environment Variables

Set this variable for the frontend deployment:

```text
VITE_API_BASE_URL=<your backend public URL>
```

Example:

```text
VITE_API_BASE_URL=https://your-api.example.com
```

Frontend build command:

```bash
npm install
npm run build
```

Frontend output directory:

```text
dist
```

## Planned Improvements

- Restore persistent history loading after page refresh.
- Add rename, delete, and pin actions for history items.
- Add optional server-side saved history.
- Add confidence calibration.
- Add line-level or block-level hybrid highlighting.
- Add file upload and batch analysis.
- Add Docker and Docker Compose configuration.
- Improve result cards with full probability bars.
- Add automated backend and frontend tests.

## Development Notes

- Backend CORS is configured by `CORS_ALLOW_ORIGINS`.
- Frontend API base URL is configured by `VITE_API_BASE_URL`.
- Model folders are required at runtime. If no valid model folder is found, the API returns an error instead of predictions.
- The repository `.gitignore` ignores model/checkpoint folders, so model weight tracking should be decided deliberately before publishing.
